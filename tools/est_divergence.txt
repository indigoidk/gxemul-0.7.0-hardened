#  THE TWIN TREES' DECLARED DIVERGENCE.  Every other src/ file must be byte-identical between
#  GXEMUL-SEC/ and est/, and precommit_check.sh section A enforces that.
#
#  This file exists because the allowlist used to be PINNED inside the checker and went stale
#  TWICE -- by one file, then by five.  Stale in the harmful direction: touching an unlisted
#  ARC/Jazz file made check A hard-fail and instruct the operator to OVERWRITE THE SEC-ONLY
#  ARC LAYER with est/'s version.  And a path listed here but NOT actually divergent
#  false-passes a genuine unpropagated edit, which is how `src/include/misc.h` sat listed
#  while being identical.
#
#  Measured 2026-08-19 by comparing every tracked src/ file.  Keep it that way: re-derive
#  rather than edit from memory.
#
#  The SEC-only ARC/Jazz enablement layer that est/ does not carry:
src/devices/.index
src/devices/Makefile.skel
src/devices/autodev.c
src/devices/dev_jazz.c
src/devices/dev_ne2000.c
src/machines/machine_arc.c
src/promemul/arcbios.c
